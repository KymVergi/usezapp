{
  "name": "usez",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/use-zbase && npm run build --workspace=apps/web",
    "build:sdk": "npm run build --workspace=packages/use-zbase",
    "build:web": "npm run build --workspace=apps/web"
  }
}